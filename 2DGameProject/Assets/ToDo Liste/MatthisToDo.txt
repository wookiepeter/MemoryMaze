 /* Diese Liste ist für dich Matthis damit du nie Ruhen wirst und immer weiter an diesem Projekt arbeiten kannst!

				
													ToDo´s   

-- Map verbessern (Zeichnen)
-> nur neu zeichnen wenn notwendig																			
-> eventuell auf ne rendertexture zeichnen und denn nur neu zeichnen wenn sich was auf der map ändert


-- #2) (Baut auf 1 auf) --> Jedes lvl was geschafft wurde, kann man übér das MainMenu nochmal spielen!
		--> "evtl" MainMenu: NeuesSpiel, Spielladen, Credits, SpielBeenden
		--> NeueSpiel  - Name: VIRUSNAME ->go!
		--> Spielladen - SpeicherplätzeAnzeigen -> MainMenuState2: SpielFortsetzen, Level(Freigeschaltete lvl),Steuerung, Spielbeenden
		--> LvL-Liste erstellen
-- #5) Traps sind nicht walkable für Ghostplayer und für Ordner!
-- #6) CurrentFocus im Player nochmal genau berechnen!(SCALE!)
-- #8) ScoreBerechner
		--> Jeder Schritt +1, ScoreItem -X, BotLöschen +count*(-1)											(DONE!)
		--> Gold,Silver,Bronze System einführen																(DONE!)
		--> Noch für jedes lvl einzeln berechnen!

-- #10) GUI Interface Bots (Anzahl(**__) und Schritte)														(CORD!)
-- #11) Wenn man den Hebel aktiviert --> Die Platte die sich öffnet oder schließt kurz grafisch anzeigen	(CORD!)
-- #11.5) Lever(blue, normal) Grafiken erstellen!															(CORD!)

-- #14) Level zu ende editieren																				(CHRIS!)
-- #15) Score für jedes Lvl fessetzen																		(CHRIS!)																
-- #19) RadiusDesSpielers(vergrößern)
-- #20) Teleporter: Spieler leuchtet von A nach B (Camera beachten! Matthis hat das schon super gemacht)	(MATTHIS)
-- #22) Bot stirb und die Punkte wandeln mit dem GLOW nach OBEN! (GEIL)										(MATTHIS WE)
-- #23) Nach jedem Level FREEZ und "Du hast xy Punkte" *** <-- das sollen Sterne sein						
-- #24) Nach jedem LevelEnde bot sterben (Punkte verrechnen!)												(CHRIS!)
-- #25) Sounds 
		- kleiner Sounds 
		- Ordner verschieben
		- Level geschafft
		- Sterben
		- Botbeschwören
		- Wechseln der Bots
		- Hebel
		- Teleporter
		- Gegen die Wand laufen/nicht passierbare Objekte
-- #26) Tutorial																							(CHRIS!)
-- #27)




													!DONE!

-- #3) TeleportationSlots erstellen (Von TileA nach TileB porten) --> Player und BlueBot <-->				(DONE!)
		--> Portal (Nur eine Richtung!) --> (Einseitig!)													(DONE!)
-- #4) Lever1 erstellen (WICHTIG!)																			(DONE!)
		--> Player/BlueBot blueBot aktivieren den Schalter(Schalter nur aktiv, wenn einer drauf ist)		(DONE!)
-- #12) BUG!: Bots werden nicht gezeichnet, wenn man die controllid von dem bot ändert(loseControll)		(DONE!)
-- #9) BlueLever(WICHTIG!)																					(DONE!)
-- #7) Detected Modus (Spieler hat verloren) z.B  trap														(DONE!)
-- #13) Nur BlueBot und Player koennen Items einsammeln!													(DONE!)
ToDo: Benny 3 Euro geben!																					(DONE!)
-- #1) Speicherplätze anlegen! (Prioriät ##2##) -> Beim ersten Start kann man den Virus einen Namen geben (So heißt das SaveGame dann) (DONE!)
-- #14) ScorePunkte nach dem Level auf 0 setzen																(DONE)
-- #16) RankingSystem einführen																				(MATTHIS!)
-- #18) MainMenu neu gestalten		
*/