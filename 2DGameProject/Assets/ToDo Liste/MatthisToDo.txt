 /* Diese Liste ist für dich Matthis damit du nie Ruhen wirst und immer weiter an diesem Projekt arbeiten kannst!

				
													ToDo´s   
ToDo: Benny 3 Euro geben!
-- Map verbessern (Zeichnen)
-> nur neu zeichnen wenn notwendig
-> eventuell auf ne rendertexture zeichnen und denn nur neu zeichnen wenn sich was auf der map ändert

-- #1) Speicherplätze anlegen! (Prioriät ##2##) -> Beim ersten Start kann man den Virus einen Namen geben (So heißt das SaveGame dann) (MATHIS!)
-- #2) (Baut auf 1 auf) --> Jedes lvl was geschafft wurde, kann man übér das MainMenu nochmal spielen!
		--> "evtl" MainMenu: NeuesSpiel, Spielladen, SpielBeenden
		--> NeueSpiel  - Name: VIRUSNAME ->go!
		--> Spielladen - SpeicherplätzeAnzeigen -> MainMenuState2: SpielFortsetzen, Level(Freigeschaltete lvl), Spielbeenden
		--> LvL-Liste erstellen
-- #5) Traps sind nicht walkebale für Ghostplayer und für Ordner!
-- #6) CurrentFocus im Player nochmal genau berechnen!(SCALE!)
-- #8) ScoreBerechner
		--> Jeder Schritt -1, ScoreItem +X, BotLöschen +count
		--> Gold,Silver,Bronze System einführen
-- #10) GUI Interface Bots (Anzahl(**__) und Schritte)														(CORD!)
-- #11) Wenn man den Hebel aktiviert --> Die Platte die sich öffnet oder schließt kurz grafisch anzeigen	(CORD!)
-- #13) Nur BlueBot und Player koennen Items einsammeln!													(MATTHIS!)

													!DONE!

-- #3) TeleportationSlots erstellen (Von TileA nach TileB porten) --> Player und BlueBot <-->				(DONE!)
		--> Portal (Nur eine Richtung!) --> (Einseitig!)													(DONE!)
-- #4) Lever1 erstellen (WICHTIG!)																			(DONE!)
		--> Player/BlueBot blueBot aktivieren den Schalter(Schalter nur aktiv, wenn einer drauf ist)		(DONE!)
-- #12) BUG!: Bots werden nicht gezeichnet, wenn man die controllid von dem bot ändert(loseControll)		(DONE!)
-- #9) BlueLever(WICHTIG!)																					(DONE!)
-- #7) Detected Modus (Spieler hat verloren) z.B  trap														(DONE!)
*/